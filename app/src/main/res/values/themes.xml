<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
      THE FIX: android:fitsSystemWindows is gone. Insets are handled in MinimalActivity with
      WindowCompat.setDecorFitsSystemWindows + an OnApplyWindowInsetsListener on the one view
      that needs it, instead of the legacy theme-level flag that Samsung mishandles.
    -->
    <style name="SamsungPaddingLossTheme" parent="Theme.AppCompat.Light.NoActionBar" />
</resources>
